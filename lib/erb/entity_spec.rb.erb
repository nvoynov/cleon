require_relative '../../test_helper'
include <%= @model.root_const %>::Entities

describe <%= @model.const %> do

  # let(:entity) { <%= @model.const %>.new(<%= @model.keyword_arguments %>) }

  it 'must do something' do
  end

end
